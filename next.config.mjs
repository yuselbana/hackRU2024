/** @type {import('next').NextConfig} */
const nextConfig = {};

 
export default {
    images: {
      domains: ['images.pexels.com', 'media.istockphoto.com'] // Add the domain of the external images
    },
  };

